interface:
  display_name: "Documenso"
  short_description: "Repo-specific patterns and workflows for documenso"
  default_prompt: "Use the documenso repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true